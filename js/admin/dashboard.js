"import { mlClient } from './mlClient.js';\n\nlet mlData = null;\n\nexport async function initAdmin() {\n    console.log('Inicializando Admin de ML...');\n    \n    // Testa se a API está rodando\n    const isOnline = await mlClient.checkConnection();\n    \n    // Mostra aviso se estiver em fallback\n    if (!isOnline) {\n        showToast('⚠️ API de ML offline. Usando dados estáticos de fallback.', 'warning');\n    }\n\n    // Busca todos os dados do ML\n    mlData = await mlClient.getMetrics();\n    \n    if (!mlData) {\n        document.getElementById('admin-content-area').innerHTML = `\n            <div class=\"empty-state\">\n                <div class=\"empty-icon\">❌</div>\n                <h3>Erro ao carregar dados de ML</h3>\n                <p>Execute <code>python ml/train_model.py</code> primeiro.</p>\n            </div>\n        `;\n        return;\n    }\n\n    // Configura os menus\n    const menuItems = document.querySelectorAll('.admin-menu-item');\n    menuItems.forEach(item => {\n        item.addEventListener('click', (e) => {\n            menuItems.forEach(m => m.classList.remove('active'));\n            e.target.classList.add('active');\n            renderView(e.target.dataset.adminView);\n        });\n    });\n\n    // Renderiza a view inicial\n    renderView('dashboard');\n}\n\nfunction renderView(viewName) {\n    const contentArea = document.getElementById('admin-content-area');\n    \n    switch(viewName) {\n        case 'dashboard':\n            contentArea.innerHTML = renderOverview();\n            break;\n        case 'ml-predict':\n            contentArea.innerHTML = renderPredict();\n            setupPredictForm();\n            break;\n        case 'ml-analysis':\n            contentArea.innerHTML = renderAnalysis();\n            break;\n        case 'ml-features':\n            contentArea.innerHTML = renderFeatureImportance();\n            break;\n        case 'ml-segments':\n            contentArea.innerHTML = renderSegments();\n            break;\n    }\n}\n\n// ==========
<truncated 13582 bytes>